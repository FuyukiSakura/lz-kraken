interface 生物 {

    /**
     * Gets if the biological is able to Full Combo
     * @returns {boolean}
     */
    fc(): boolean;

    /**
     * Gets if the biological is able to Full Combo
     * @returns {boolean}
     */
    ap(): boolean;

    /**
     * Gets if the biological can fail
     * @returns {boolean}
     */
    fail(): boolean;
}